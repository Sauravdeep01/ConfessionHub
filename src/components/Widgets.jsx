import './Widgets.css';

function Widgets({ confessions = [], onSearch }) {
    return (
        <aside className="widgets-sidebar">
            {/* Widgets removed - Hot Topics and Community Mood have been deleted */}
        </aside>
    );
}

export default Widgets;
